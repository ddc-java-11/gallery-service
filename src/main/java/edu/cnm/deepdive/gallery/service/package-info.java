/**
 * Classes and interfaces defining operations available throughout the application (consumed
 * primarily by controllers), implementing security policies, business logic, and persistence.
 * Note that JPA-based persistence of entity class instances is provided by Spring Data
 * implementations of the interfaces in the {@link edu.cnm.deepdive.gallery.model.dao} package,
 * which are in turn consumed by the services in this package.
 */
package edu.cnm.deepdive.gallery.service;